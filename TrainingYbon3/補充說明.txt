
Must Fix
在有 showCount() 的版本中（以下簡稱 A 版）部分的 function 只 call 了一次，且程式碼非常短少，
卻也獨立出來成了一支 function，將這些 function 精簡掉這是我覺得可以改善「程式碼品質」的一個重點，
這讓程式碼在閱讀上不需要在 function 之間跳來跳去。

然而 showCount() 這支 function 在作用上來說獨立成 function 


在 b3c40ba 的 commit 中：
不論是 innerHTML 或者是對 DOM 的相關操作，對於整個 HTML Document 的 tree 結構影響都很大，
並且會導致畫面的 rerender


1. 將過度使用對 DOM 的操作方式移除
2. 不再使用 innerHTML 屬性，我僅僅只是知道 innerHTML 屬性的使用是一種較沒效率的操作


> 不，以前有 showCount() 的版本是比較好的，現在改成這樣就是「為了精簡程式碼反而造成程式碼品質降低」的經典例子
有 showCount() 的版本中，移除那些只被 call 了一次且不應再被其他地方使用的 method 我覺得確實有助於改善程式的可讀性，
但 showCount() 這支 method 除外，將 showCount() 獨立出來另外定義成 method 對於日後要改變顯示上的做法會比較有彈性。
同樣的，日後若要改變原本獨立出來的 method（如 randomsort()）進行擴充（選擇不同的演算法來進行數組打散），
等到時候再獨立出來也可以。

> 即使有些 method 被刻意精簡了...



> 「不直接用 div 是因為會導致排版換行」這種說法根本是無稽之談
我只是試著回答你原本提出的疑問，這是在第一版寫法為背景的考量下做出的選擇。
或許我這個想法有欠思量，又或許因為我對 HTML 或 DOM 操作等知識的不足導致做了這個不適當的選擇，
但這確實是我原本之所以沒使用一個 div 當作 empty 的原因，
如果這選擇有甚麼問題，請你說出其中我疏忽的部分，而非丟一個無稽之談來打發我。

> 你的 bonus 回答... 非常表面。我希望知道的是「你知不知道對底層有什麼影響」，而不是帳面上看 diff 就能夠說的東西
由於改變 DOM 會影響 DOM tree 進而引發畫面上的 rerender，所以能避免對於 DOM 的操作就應該盡量避免；
innerHTML 則可能引發大量的 event 以及 validation，也會影響 DOM tree，應該盡可能避免。

> 跟你之前的寫法比起來，作 <body onload="foo()"> 是比較不好的寫法吧？為什麼要（為了減少程式碼）越改越糟呢？
使用 onload 取代原本的 (function(){})() 寫法，起初的目的確實是為了減少程式碼，
但同時也發現 onload 會在 HTML 文件將其他內容（如 image）完成載入後才觸發，
在這次的拼圖遊戲中我認為這是比較好的選擇，目前沒理由認為這是比較不好的寫法，
至少在這一次的 Training 中我看不出來有什麼缺點，可以明確指出嗎？
